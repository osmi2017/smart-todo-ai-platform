PORT=4000
FRONTEND_URL=http://localhost:3000

# Bus d'événements Kafka — consomme "meeting_started" pour pré-provisionner
# la salle WebRTC, totalement découplé du backend Django.
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_EVENTS_ENABLED=true
